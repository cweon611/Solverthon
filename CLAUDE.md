@AGENTS.md

# 브릿지(Bridge) 프로젝트 규칙

- 구현 명세는 `브릿지_PRD.md`(v2.0)다. 코드를 쓰기 전에 §0(절대 규칙) → §2(현재 상태) → §4(디자인 통합) → §11(구현 순서)를 읽고, Phase 순서대로 진행한다.
- 디자인 원본은 `design/BridgePage.tsx`, `design/globals.css`다. 시각 스타일(색·간격·타이포·컴포넌트 구조)은 바꾸지 않는다(PRD §4.1). 앱 코드에서 `design/`을 import하지 않는다.
- 판정·마감일·법령 해석은 LLM에 시키지 않는다. `lib/engine/*`의 결정론 코드만 담당한다(PRD §0.1-1, §3.1).
- `.env.local`의 값은 출력·로그·커밋하지 않는다. 키 이름 확인은 `grep -vE '^\s*(#|$)' .env.local | cut -d= -f1`.
- 공고 수집은 공식 오픈 API만 사용한다. 기관 누리집 크롤링 코드를 작성하지 않는다(대회 규정).
- 문서와 코드가 충돌하면 코드만 조용히 바꾸지 말고 PRD를 고치고 사람에게 알린다.
